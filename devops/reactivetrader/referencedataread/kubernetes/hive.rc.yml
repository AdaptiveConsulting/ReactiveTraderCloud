apiVersion: v1
kind: ReplicationController
metadata:
  name: "referencedataread-<% reactivetrader.servers.major %>-<% reactivetrader.servers.minor %>-<% cli.id %>"
  labels:
    name: "referencedataread"
    major: "<% reactivetrader.servers.major %>"
    minor: "<% reactivetrader.servers.minor %>"
    build: "<% cli.id %>"
spec:
  replicas: 1
  selector:
    name: "referencedataread"
    major: "<% reactivetrader.servers.major %>"
    minor: "<% reactivetrader.servers.minor %>"
    build: "<% cli.id %>"
  template:
    metadata:
      labels:
        name: "referencedataread"
        major: "<% reactivetrader.servers.major %>"
        minor: "<% reactivetrader.servers.minor %>"
        build: "<% cli.id %>"
    spec:
      containers:
      - name: "referencedataread"
        image: "<% reactivetrader.servers.image %>:<% reactivetrader.servers.major %>.<% reactivetrader.servers.minor %>.<% cli.id %>"
        command: [ "dnx", "--configuration", "Release", "-p", "Adaptive.ReactiveTrader.Server.ReferenceDataRead", "prod" ]
        imagePullPolicy: IfNotPresent
