apiVersion: v1
kind: ReplicationController
metadata:
  name: "analytics-<% reactivetrader.servers.major %>-<% reactivetrader.servers.minor %>-<% cli.id %>"
  labels:
    name: "analytics"
    major: "<% reactivetrader.servers.major %>"
    minor: "<% reactivetrader.servers.minor %>"
    build: "<% cli.id %>"
spec:
  replicas: 1
  selector:
    name: "analytics"
    major: "<% reactivetrader.servers.major %>"
    minor: "<% reactivetrader.servers.minor %>"
    build: "<% cli.id %>"
  template:
    metadata:
      labels:
        name: "analytics"
        major: "<% reactivetrader.servers.major %>"
        minor: "<% reactivetrader.servers.minor %>"
        build: "<% cli.id %>"
    spec:
      containers:
      - name: "analytics"
        image: "<% reactivetrader.servers.image %>:<% reactivetrader.servers.major %>.<% reactivetrader.servers.minor %>.<% cli.id %>"
        imagePullPolicy: IfNotPresent
        command: [ "dnx", "--configuration", "Release", "-p", "Adaptive.ReactiveTrader.Server.Analytics", "prod"]
