machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
    - deploy/docker/build/servers/dnxcache
    - deploy/docker/build/web/.npm
  override:
    - docker info
    - cd deploy/CI && chmod 755 ensurePermissions && ./ensurePermissions
    - cd deploy/CI && ./loadContainers
    - cd deploy/CI && ./buildContainers $CIRCLE_BUILD_NUM
    - cd deploy/CI && ./runContainers $CIRCLE_BUILD_NUM
    - cd deploy/CI && ./saveContainers

test:
  override:
    - docker ps
    - for i in {1..10}; do curl http://localhost:2113/web/index.html && break; sleep 1; if [[ $i == 10 ]];then false;fi; done
    - . ./deploy/config && docker run -a STDOUT --net=host $serversContainer.$CIRCLE_BUILD_NUM bash -c "dnx --configuration Release -p Adaptive.ReactiveTrader.Server.IntegrationTests test -parallel none"

deployment:
  hub:
    branch: [master, Clean-Deploy]
    commands:
      - cd deploy/CI && ./pushContainers $CIRCLE_BUILD_NUM
