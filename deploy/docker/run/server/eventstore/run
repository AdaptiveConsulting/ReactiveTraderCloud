#! /bin/bash

echo "EventStore"

. ../../../../config

docker rm eventstore 2&> garbage

docker run -d --net=host            \
  --name eventstore                 \
  $eventstoreContainer:$vEventstore \
  > container_id

rm garbage
echo "  started"

./populate
echo " "
