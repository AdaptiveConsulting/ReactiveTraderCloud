#! /bin/bash

echo "Web"

. ../../../config

docker rm web 2&> garbage

docker run -d --net=host                   \
     --name web                            \
     $webContainer:$vMajor.$vMinor.$vBuild
     > container_id 

rm garbage

echo "  started"
echo " "
