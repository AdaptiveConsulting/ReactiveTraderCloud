name: Pull Request (origin, up)

on:
  pull_request:
    types: [labeled, opened, reopened, synchronize]
    branches:
      - master
      - release/**

env:
  DEPLOY_ENV: 'pr${{ github.event.number }}'

jobs:
  build_and_deploy:
    runs-on: ubuntu-20.04

    if: "!github.event.pull_request.head.repo.fork"

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Set up Google Cloud
        uses: google-github-actions/setup-gcloud@master
        with:
          version: '285.0.0'
          project_id: ${{ secrets.GOOGLE_PROJECT_ID }}
          service_account_key: ${{ secrets.GOOGLE_SERVICE_KEY }}

      - name: 'Build, push & deploy'
        env:
          DOCKER_PASS: ${{ secrets.DOCKER_PASS }}
          GKE_CLUSTER: ${{ secrets.GOOGLE_CLUSTER }}
          GCP_COMPUTE_ZONE: ${{ secrets.GOOGLE_COMPUTE_ZONE }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: ./deploy/build-and-deploy.sh ${{ env.DEPLOY_ENV }}

      - name: Find deployment comment
        uses: peter-evans/find-comment@v1
        id: find_comment
        with:
          issue-number: ${{ github.event.number }}
          comment-author: 'github-actions[bot]'
          body-includes: '(auto-deploy)'

      - name: Create deployment comment if not exists
        if: ${{ steps.find_comment.outputs.comment-id == 0 }}
        uses: peter-evans/create-or-update-comment@v1
        with:
          issue-number: ${{ github.event.number }}
          body: |
            (auto-deploy) A deployment has been created for this Pull Request.
            https://${{ env.DEPLOY_ENV }}.lb.adaptivecluster.com

      - name: Update deployment comment if exists
        if: ${{ steps.find_comment.outputs.comment-id != 0 }}
        uses: peter-evans/create-or-update-comment@v1
        with:
          comment-id: ${{ steps.find_comment.outputs.comment-id }}
          body: |
            (auto-deploy) A deployment has been created for this Pull Request.
            https://${{ env.DEPLOY_ENV }}.lb.adaptivecluster.com
          edit-mode: "replace"

