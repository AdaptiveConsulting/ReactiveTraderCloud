VITE_BROKER_HOST=dev.reactivetrader.com
VITE_BROKER_PORT=443
ENVIRONMENT_NAME=local

# The VITE_ANALYTICS_URL var is only used from the OF launcher, so most of the times we'll want to 
# launch the live app even when doing dev work on the launcher itself. Otherwise switch to the localhost one
VITE_ANALYTICS_URL=https://demo-reactive-analytics.adaptivecluster.com/
# VITE_ANALYTICS_URL=http://localhost:3005

# This will be injected by GitHub workflow
BASE_URL=http://localhost:1917