describe('Analytics epics', () => {
  it('should call the getAnalyticsStream with currency as the argument', () => {
    expect(1).toBe(1)
  })

  it('should ', () => {
    expect(1).toBe(1)
  })

  it('should listen to subscribe as long as connection is still alive', () => {
    expect(1).toBe(1)
  })
})
